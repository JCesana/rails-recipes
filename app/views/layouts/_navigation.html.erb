<nav class="navbar navbar-default">
  <div class="navbar-brand">
    <%= link_to "Rails Recipes", root_path %>
  </div>

  <div class="navbar-end">
    <div class="navbar-item">
      <div class="buttons">

      <% if user_signed_in? %>

        <a class="button is-primary" href="<%= new_recipe_path %>">
          <strong>New Recipe</strong>
        </a>
        <a class="button is-light" href="<%= destroy_user_session_path %>" data-method="delete">
          Sign Out
        </a>

      <% else %>

        <a class="button is-primary" href="<%= new_user_registration_path %>">
          <strong>Sign Up</strong>
        </a>
        <a class="button is-light" href="<%= new_user_session_path %>">
          Sign In
        </a>

      <% end %>

      </div>
    </div>
  </div>
</nav>
